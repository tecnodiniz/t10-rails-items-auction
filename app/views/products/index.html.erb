<%= link_to "Cadastrar produto", new_product_path %>

<%@products.each do |product|%>
 <%= link_to product.cod, product_path(product) %> <%= link_to product.name, product_path(product) %> <%= t("activerecord.enum.product.status.#{product.status}") %>


<%end%>