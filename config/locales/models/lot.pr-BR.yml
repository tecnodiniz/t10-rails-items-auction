pt-BR:
  activerecord:
    models:
      lot:
        one: Lote
        other: Lotes
    attributes:
      lot:
        code: Código
        start_date: Data de início
        limit_date: Data limite
        min_value: Lance mínimo
        dif_value: Diferença de lance
    enum:
      lot:
        status:
          awaiting: aguardando aprovação
          aproved: aprovado
          finished: encerrado
